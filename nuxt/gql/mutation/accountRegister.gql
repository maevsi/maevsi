mutation($username: String!, $password: String!, $emailAddress: String!) {
  accountRegister(
    input: {
      username: $username
      password: $password
      emailAddress: $emailAddress
    }
  ) {
    clientMutationId
    jwt
  }
}
