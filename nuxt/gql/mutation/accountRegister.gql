mutation($emailAddress: String!, $password: String!, $username: String!) {
  accountRegister(
    input: {
      emailAddress: $emailAddress
      password: $password
      username: $username
    }
  ) {
    clientMutationId
  }
}
