#import "../../fragments/contactFields.gql"

mutation ContactUpdateById($id: BigInt!, $contactPatch: ContactPatch!) {
  updateContactById(input: { id: $id, contactPatch: $contactPatch }) {
    contact {
      ...contactFields
    }
  }
}
