mutation($organizerUsername: String!, $slug: String!, $password: String!) {
  eventDelete(
    input: { organizerUsername: $organizerUsername, slug: $slug, password: $password }
  ) {
    event {
      id
      nodeId
    }
    clientMutationId
  }
}
