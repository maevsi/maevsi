mutation($organizerUsername: String!, $password: String!, $slug: String!) {
  eventDelete(
    input: {
      organizerUsername: $organizerUsername
      password: $password
      slug: $slug
    }
  ) {
    clientMutationId
    event {
      id
      nodeId
    }
  }
}
